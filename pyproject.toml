[project]
name = "comfyui-prompt-workbench"
version = "0.1.0"
description = "Structured prompt workbench for ComfyUI"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
