[project]
name = "comfyui-prompt-all-in-one"
version = "0.1.0"
description = "Structured positive and negative prompt editor for ComfyUI"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
