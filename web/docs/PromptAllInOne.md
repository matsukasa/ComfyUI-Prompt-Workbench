# Prompt All-in-One

Positive / Negativeプロンプトをタグ単位で編集し、2つのSTRINGとして出力します。

1. タブを選び、追加欄へカンマまたは改行区切りで入力します。
2. タグをドラッグして並べ替えます。
3. チェックボックス、Ctrl/Cmd、Shiftで複数選択します。
4. `positive` / `negative`を`CLIP Text Encode`の`text`へ接続します。

無効タグは保存されますが出力されません。翻訳APIキーはワークフローへ保存されません。
