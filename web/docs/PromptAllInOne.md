# Prompt All-in-One

1本のプロンプトをタグ単位で編集し、STRINGとしてそのまま出力します。

1. 追加欄へカンマまたは改行区切りで入力します。
2. タグをドラッグして並べ替えます。
3. チェックボックス、Ctrl/Cmd、Shiftで複数選択します。
4. `prompt`出力を、必要な場所の`CLIP Text Encode`などへ接続します。

無効タグは保存されますが出力されません。翻訳APIキーはワークフローへ保存されません。
